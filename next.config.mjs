// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Example:
  // reactStrictMode: true,
  // experimental: {
  //   appDir: true, // If you are using the App Router
  // },
};

export default nextConfig;